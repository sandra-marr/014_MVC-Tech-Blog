<!-- Loop over each object in the posts array -->
{{#each posts as |post| }}
<div class="row mb-4 post border display-felx justify-space-between">
  <div>
    <h2>
      <a href="/api/post/{{post.id}}">{{post.post_title}}</a>
    </h2>
  </div>
  <div>
    <p class="side-note">Created by {{post.user.user_name}} on {{format_date  post.post_date}}</p>
  </div>
  <div class="col-md-12">
    <p>
      {{post.post_body}}
    </p>
  </div>
</div>

{{/each}}
